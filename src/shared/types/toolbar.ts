import type {
  ToolbarPlacementMode as BaseToolbarPlacementMode,
  ToolbarModesByGroup as BaseToolbarModesByGroup,
  ToolbarToolMode as BaseToolbarToolMode,
} from "./actionmode";

export type ToolbarPlacementMode = BaseToolbarPlacementMode;
export type ToolbarModesByGroup = BaseToolbarModesByGroup;
export type ToolbarToolMode = BaseToolbarToolMode;
